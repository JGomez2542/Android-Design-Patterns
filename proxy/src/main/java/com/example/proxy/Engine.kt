package com.example.proxy

import android.util.Log

class Engine {

    private val TAG = "BoatEngine"

    fun startBoatEngine() {
        Log.d(TAG, "Starting boat engine")
    }

    fun stopBoatEngine() {
        Log.d(TAG, "Stopping boat engine")
    }
}